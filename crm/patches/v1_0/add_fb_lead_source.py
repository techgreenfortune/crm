def execute():
	# No-op (was: insert "Facebook" CRM Lead Source). Retired when source
	# attribution was scoped to the 8 IndiFrame sources (PRD §7). The patch
	# entry stays in patches.txt so older sites that already applied it
	# remain consistent; new sites get nothing from this step.
	pass
